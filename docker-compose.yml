services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: havenhold
      POSTGRES_PASSWORD: havenhold
      POSTGRES_DB: havenhold
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
